{
  "artifactStructure": {
    "assets": [
      {
        "root": "storybook-static"
      }
    ],
    "hbsTemplates": []
  },
  "baseImageTag": "node-16",
  "dependencies": {
    "installDepsCmd": "echo $npm_config_auth && npm install && npm run build:js && npm run build:css",
    "requiredFiles": [
      ".nvmrc",
      "package.json",
      "src/index.ts",
      "tailwind.config.cjs",
      "tailwind-no-resets.config.cjs"
    ]
  },
  "buildArtifacts": {
    "buildSetupCmd": "npm ci",
    "buildCmd": "npm run build-storybook"
  },
  "livePreview": {
      "serveCmd": "npm run storybook -- -p 8080"
  }
}
