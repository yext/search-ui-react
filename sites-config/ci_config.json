{
  "artifactStructure": {
    "assets": [
      {
        "root": "storybook-static"
      }
    ],
    "hbsTemplates": []
  },
  "baseImageTag": "node-14",
  "dependencies": {
    "installDepsCmd": "echo $npm_config_auth && npm install",
    "requiredFiles": []
  },
  "buildArtifacts": {
    "buildSetupCmd": "nvm version && npm ci",
    "buildCmd": "nvm version && npm run build-storybook"
  },
  "livePreview": {
      "serveCmd": "npm run storybook -p 8080"
  }
}